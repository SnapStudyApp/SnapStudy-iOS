/*
 Camera Page Layout:
 
 Includes access to back camera so that users
 can capture object that needs to be translated.
 More features with further updates.
*/

import SwiftUI

struct CameraView: View {
    var body: some View {
        Text("Camera Feed")
    }
}

struct CameraView_Previews: PreviewProvider {
    static var previews: some View {
        CameraView()
    }
}
